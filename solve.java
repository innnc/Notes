qualifiers solve (parameters)
{
	declarations
    if (simple to solve)
    	return the answer
    else {
        simplify the problem
        invoke solve on simpler problem
        calculate answer (at this level)
        return the answer
    }
}
